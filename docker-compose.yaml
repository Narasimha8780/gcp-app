version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9090:9090"   # host:container
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
